"""
Medical QA System - Python Model State Package.

Contains dataclass wrappers that hold the runtime state of loaded ML models.
These are distinct from the AI model weights themselves; they are lightweight
metadata carriers used internally by the services.
"""
